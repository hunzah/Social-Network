import React from 'react';
import {StateType} from '../redux/store';

const mapStateToProps= (state:StateType) => {
    return {

    }
}
const mapDispatchToProps = {

}

export const UsersContainer = (mapStateToProps , mapDispatchToProps)(Users)